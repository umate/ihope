<%= form_for(resource, :as => resource_name, :url => session_path(resource_name), html: { class: 'form-signin'} ) do |f| %>
  <h4 class="form-signin-heading">Вход в панель управления</h4>

  <%= f.email_field :email, :autofocus => true, class: 'input-block-level', placeholder: 'Email адрес' %>

  <%= f.password_field :password, class: 'input-block-level', placeholder: 'Пароль' %>

  <% if devise_mapping.rememberable? -%>
    <label class="checkbox">
      <%= f.check_box :remember_me, class: 'remember-me' %> <%= f.label 'Запомнить меня' %>
    </label>
  <% end -%>

  <%= f.button "Войти", type: 'submit', class: 'btn btn-primary' %>
<% end %>