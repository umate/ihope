= rails_admin_form_for @user_email, :url => bulk_send_email_path(:model_name => @abstract_model.to_param, :bulk_ids => @objects.map(&:id)), \
                                    :html => { method: 'put', :multipart => true, :class => "form-horizontal denser", \
                                               :data => { :title => @page_name } } do |form|
  = form.generate :action => :create, model_config: RailsAdmin::Config.model(@user_email), submit_text: t('admin.form.send')