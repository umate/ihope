= rails_admin_form_for @user_email, :url => send_email_path(@abstract_model, @object.id), \
                                    :html => { method: 'post', :multipart => true, :class => "form-horizontal denser", \
                                               :data => { :title => @page_name } } do |form|
  = form.generate :action => :create, model_config: RailsAdmin::Config.model(@user_email), submit_text: t('admin.form.send')